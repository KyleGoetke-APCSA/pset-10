public class Main extends Application {
    Stage window;
    Button button;
    static WebView webview;
    static WebEngine webengine;

    @Override
    public void start(Stage primaryStage) throws Exception {
//        WebView browser = new WebView();
//        WebEngine engine = browser.getEngine();
//        File f = new File("/html/test.html");
//        engine.load(getClass().getResource("/html/test.html").toExternalForm());
    }

    public static void initialize(WebEngine webengine, WebView webview) {
        webengine = webview.getEngine();
        webengine.loadContent("http://www.oracle.com/us/products/index.html");
    }

    public static void main(String[] args) {
        launch(args);
        initialize(webengine, webview);
    }
}
